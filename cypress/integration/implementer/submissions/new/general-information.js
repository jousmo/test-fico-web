/*
describe("Submission new general information page", () => {
  /!*
  * Visits the page before each test
  *!/
  beforeEach(() => {
    cy.log(`Visiting http://localhost:3000/implementer/submissions
    /new/general-information`)
    cy.visit("/implementer/submissions/new/general-information")
  })

  it("Should have steps bar", () => {
    cy.get(".ant-layout-content")
      .get(".ant-page-header-content")
      .get(".ant-page-header-heading-title")
      .should("have.text", "Nueva solicitud")

    cy.get(".ant-layout-content")
      .get(".ant-page-header-content")
      .get(".ant-steps > .ant-steps-item")
      .eq(0)
      .should("have.class", "ant-steps-item-active")
  })
})
*/
